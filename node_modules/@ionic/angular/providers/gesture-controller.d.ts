import { Gesture, GestureConfig } from '@ionic/angular/providers/node_modules/@ionic/core';
export declare class GestureController {
    /**
     * Create a new gesture
     */
    create(opts: GestureConfig): Gesture;
}
