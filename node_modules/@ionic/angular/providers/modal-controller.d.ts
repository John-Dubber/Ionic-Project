import { ComponentFactoryResolver, Injector } from '@ionic/angular/providers/node_modules/@angular/core';
import { ModalOptions } from '@ionic/angular/providers/node_modules/@ionic/core';
import { OverlayBaseController } from '@ionic/angular/util/overlay';
import { AngularDelegate } from '@ionic/angular/providers/angular-delegate';
export declare class ModalController extends OverlayBaseController<ModalOptions, HTMLIonModalElement> {
    private angularDelegate;
    private resolver;
    private injector;
    constructor(angularDelegate: AngularDelegate, resolver: ComponentFactoryResolver, injector: Injector);
    create(opts: ModalOptions): Promise<HTMLIonModalElement>;
}
